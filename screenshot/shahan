#!/bin/bash
# ====================== Colors ======================
RED="\e[31m"
GREEN="\e[32m"
YELLOW="\e[33m"
BLUE="\e[34m"
CYAN="\e[36m"
MAGENTA="\e[35m"
BOLD="\e[1m"
ENDCOLOR="\e[0m"
# ====================== System Info ======================
ipv4=$(curl --connect-timeout 5 -s ipv4.icanhazip.com)
ipv6=$(curl --connect-timeout 5 -s ipv6.icanhazip.com 2>/dev/null || echo "Not Available")
po=$(grep "^Port" /etc/ssh/sshd_config)
port=${po#Port }
panelversion=$(cat /var/www/html/p/versionp.txt 2>/dev/null || echo "Unknown")
panelport=$(sudo lsof -i -P -n | grep apache2 | awk '{print $9}' | head -n1 | sed 's/[^0-9]*//g')
adminuser=$(mysql -s -N -e "use ShaHaN; SELECT adminuser FROM setting WHERE id='1';")
adminpass=$(mysql -s -N -e "use ShaHaN; SELECT adminpassword FROM setting WHERE id='1';")
usercount=$(mysql -s -N -e "use ShaHaN; SELECT COUNT(*) FROM users;")
cpuarch=$(uname -i)
linuxversion=$(lsb_release -sd 2>/dev/null || echo "Unknown")
uninstall_shahan(){
    printf "Are you sure uninstall Shahan? (y/n) "
    printf "\n"
    read -p "(Default: n):" answer
    [ -z ${answer} ] && answer="n"
    if [ "${answer}" == "y" ] || [ "${answer}" == "Y" ]; then
        rm -fr /var/www/html/*
        mysql -e "drop database ShaHaN;"
        clear
        echo "Shahan uninstall successfully!"
    else
        clear
        echo
        echo "uninstall cancelled, nothing to do..."
        echo
    fi
}
uninstall_openvpn(){
systemctl stop ovpmd
apt remove ovpm -y 
rm -fr /var/db/ovpm
rm -fr /var/www/html/p/open
clear
echo "OpenVpn uninstall successfully!"
}
uninstall_tuic(){
systemctl stop tuic
rm -fr /var/www/config.json
rm -fr /etc/systemd/system/tuic.service
rm -fr /root/tuic
clear
echo "TUIC uninstall successfully!"
}
uninstall_shadowsocks(){
    printf "Are you sure uninstall Shadowsocks? (y/n) "
    printf "\n"
    read -p "(Default: n):" answer
    [ -z ${answer} ] && answer="n"
    if [ "${answer}" == "y" ] || [ "${answer}" == "Y" ]; then
        ps -ef | grep -v grep | grep -i "ssserver" > /dev/null 2>&1
        if [ $? -eq 0 ]; then
            /etc/init.d/shadowsocks stop
        fi
        # delete config file
        rm -f /var/www/shadowsocks.json
        rm -f /var/run/shadowsocks.pid
        rm -f /etc/init.d/shadowsocks
        rm -f /var/log/shadowsocks.log
        if [ -f /usr/local/shadowsocks_install.log ]; then
            cat /usr/local/shadowsocks_install.log | xargs rm -rf
        fi
        clear
        echo "Shadowsocks uninstall success!"
    else
        clear
        echo
        echo "uninstall cancelled, nothing to do..."
        echo
    fi
}
if ! [ $(id -u) = 0 ]; then
   echo -e "${RED}Plese run the command with root privilages!${ENDCOLOR}"
   exit 1
fi
# ====================== Pretty Tables ======================
clear
echo -e "${BOLD}${CYAN}╔═══════════════════════════════════════════════════════╗${ENDCOLOR}"
echo -e "${BOLD}${CYAN}║               ${MAGENTA}ShaHaN SSH Panel${CYAN}                        ║${ENDCOLOR}"
echo -e "${BOLD}${CYAN}╚═══════════════════════════════════════════════════════╝${ENDCOLOR}"
echo -e "\n${YELLOW}┌─ Server Information ────────────────────────────────┐${ENDCOLOR}"
echo -e "  ${GREEN}OS          :${ENDCOLOR} $linuxversion ($cpuarch)"
echo -e "  ${GREEN}IPv4        :${ENDCOLOR} $ipv4"
echo -e "  ${GREEN}IPv6        :${ENDCOLOR} $ipv6"
echo -e "  ${GREEN}Panel Link  :${ENDCOLOR} http://$ipv4:$panelport/p"
echo -e "  ${GREEN}Admin User      :${ENDCOLOR} $adminuser"
echo -e "  ${GREEN}Admin Pass      :${ENDCOLOR} $adminpass"
echo -e "  ${GREEN}Panel Version   :${ENDCOLOR} $panelversion"
echo -e "${YELLOW}└─────────────────────────────────────────────────────┘${ENDCOLOR}"
# ====================== Menu ======================
echo -e "  ${GREEN}1${ENDCOLOR}) Install & Update Panel     ${RED}2) Remove ShaHaN ${ENDCOLOR}"
echo -e "  ${GREEN}3${ENDCOLOR}) Install SSL For Domain     ${GREEN}4${ENDCOLOR}) Change SSH Port"
echo -e "  ${GREEN}5${ENDCOLOR}) Block Iran IP              ${GREEN}6${ENDCOLOR}) Change Panel Port"
echo -e "  ${GREEN}7${ENDCOLOR}) Backup Users               ${GREEN}8${ENDCOLOR}) Check Status"
echo -e "\n${YELLOW}─── Install Options ─────────────────────────────${ENDCOLOR}"
echo -e "  ${GREEN}10${ENDCOLOR}) Install Cisco Anyconnect   ${GREEN}11${ENDCOLOR}) Install DropBear"
echo -e "  ${GREEN}12${ENDCOLOR}) Install TUIC               ${GREEN}13${ENDCOLOR}) Install OpenVPN"
echo -e "  ${GREEN}14${ENDCOLOR}) Install Fake Website       ${GREEN}15${ENDCOLOR}) Install BBR"
echo -e "  ${GREEN}16${ENDCOLOR}) Install Shadowsocks        ${GREEN}17${ENDCOLOR}) Install WireGuard"
echo -e "  ${GREEN}18${ENDCOLOR}) Install Singbox            ${GREEN}19${ENDCOLOR}) Install SSH-TLS"
echo -e "\n${RED}─── Remove Options ────────────────────────────────${ENDCOLOR}"
echo -e "  ${RED}20${ENDCOLOR}) Remove Cisco Anyconnect    ${RED}21${ENDCOLOR}) Remove SSH-TLS"
echo -e "  ${RED}22${ENDCOLOR}) Remove Apache & PHP        ${RED}23${ENDCOLOR}) Remove Nethogs"
echo -e "  ${RED}24${ENDCOLOR}) Remove SSL                 ${RED}25${ENDCOLOR}) Remove OpenVPN"
echo -e "  ${RED}26${ENDCOLOR}) Remove Singbox             ${RED}27${ENDCOLOR}) Remove Shadowsocks"
echo -e "  ${RED}28${ENDCOLOR}) Remove TUIC                ${RED}29${ENDCOLOR}) Remove WireGuard"
echo -e "\n${CYAN}─── Utilities ─────────────────────────────────────${ENDCOLOR}"
echo -e "  ${GREEN}30${ENDCOLOR}) Enable AutoUpdate       ${RED}31${ENDCOLOR}) Disable AutoUpdate"
echo -e "  ${GREEN}32${ENDCOLOR}) Enable IPv6             ${RED}33${ENDCOLOR}) Disable IPv6"
echo -e "  ${GREEN}34${ENDCOLOR}) CPU Cooler              ${GREEN}35${ENDCOLOR}) Free Domain"
echo -e "\n  ${RED}0${ENDCOLOR}) Exit"
echo -ne "\n${BOLD}${GREEN}Select Operation : ${ENDCOLOR}"
read -r n
# ====================== Case Menu ======================
case $n in
  1) bash <(curl -Ls https://raw.githubusercontent.com/HamedAp/Ssh-User-management/master/install.sh) ;;
  2) uninstall_shahan ;;
  3) bash <(curl -Ls https://raw.githubusercontent.com/HamedAp/Ssh-User-management/master/ssl.sh) ;;
  4) echo -ne "${YELLOW}Enter new SSH port: ${ENDCOLOR}"; read new_port
     sudo sed -i "s/^Port .*/Port $new_port/" /etc/ssh/sshd_config
     systemctl restart sshd
     echo -e "${GREEN}SSH Port changed to $new_port successfully!${ENDCOLOR}" ;;
  5) bash <(curl -Ls https://raw.githubusercontent.com/HamedAp/Ssh-User-management/master/b.sh) ;;
  6) bash <(curl -Ls https://raw.githubusercontent.com/HamedAp/Ssh-User-management/master/screenshot/chp.sh) ;;
  7) mysqldump -u root ShaHaN > "/root/ShaHaN_Backup_$(date +%F).sql" && clear && echo -e "${GREEN}Backup created successfully!${ENDCOLOR}" && echo -e "/root/ShaHaN_Backup_$(date +%F).sql";;
  8) shahancheck ;;
  10) bash <(curl -Ls https://raw.githubusercontent.com/HamedAp/Ocserv-Ubuntu/main/c.sh) ;;
  11) bash <(curl -Ls https://raw.githubusercontent.com/HamedAp/Ssh-User-management/main/drop.sh) ;;
  12) bash <(curl -Ls https://raw.githubusercontent.com/HamedAp/Ssh-User-management/main/t.sh) ;;
  13) bash <(curl -Ls https://raw.githubusercontent.com/HamedAp/Ssh-User-management/main/open.sh) ;;
  14) bash <(curl -Ls https://raw.githubusercontent.com/HamedAp/Ssh-User-management/main/fakewebsite.sh) ;;
  15) bash <(curl -Ls https://raw.githubusercontent.com/HamedAp/Ssh-User-management/main/bbr.sh) ;;
  16) /root/shadow.sh.x ;;
  17) /root/wg.sh.x ;;
  18) /root/signbox.sh.x ;;
  19) bash <(curl -Ls https://raw.githubusercontent.com/HamedAp/Ssh-User-management/main/tls.sh) ;;
  20) bash <(curl -Ls https://raw.githubusercontent.com/HamedAp/Ocserv-Ubuntu/main/rc.sh) ;;
  21) bash <(curl -Ls https://raw.githubusercontent.com/HamedAp/Ssh-User-management/main/ssh-tls-remove.sh) ;;
  22) apt purge apache2 php* -y && apt autoremove -y ;;
  23) crontab -l | grep -v 'posttraffic.php' | crontab - && killall -9 nethogs &&  echo -e "Nethogs Removed From Panel";;
  24) bash <(curl -Ls https://raw.githubusercontent.com/HamedAp/Ssh-User-management/main/removessl.sh) ;;
  25) uninstall_openvpn ;;
  26) bash <(curl -Ls https://raw.githubusercontent.com/HamedAp/Ssh-User-management/main/removesignbox.sh) ;;
  27) uninstall_shadowsocks ;;
  28) uninstall_tuic ;;
  29) /root/wg.sh.x remove ;;
  30) (crontab -l ; echo '4 4 * * * /bin/bash -c "sudo bash /root/updateshahan.sh"') | crontab - ;;
  31) crontab -l | grep -v 'updateshahan.sh' | crontab - ;;
  32) bash <(curl -Ls https://raw.githubusercontent.com/HamedAp/Ssh-User-management/main/enableipv6.sh) ;;
  33) bash <(curl -Ls https://raw.githubusercontent.com/HamedAp/Ssh-User-management/main/disableipv6.sh) ;;
  34) bash <(curl -Ls https://raw.githubusercontent.com/HamedAp/Ssh-User-management/main/cpu.sh) ;;
  35) bash <(curl -Ls https://raw.githubusercontent.com/HamedAp/Ssh-User-management/main/free-domain.sh) ;;
  99) /root/updatesignbox.sh.x ;;
  0) clear; exit ;;
  *) echo -e "${RED}Invalid option!${ENDCOLOR}"; read -p "Press Enter to continue..."; shahan ;;
esac
